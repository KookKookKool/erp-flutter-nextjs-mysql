# Flutter Environment Configuration

# API Configuration
# Development API URL (localhost)
DEV_API_URL=http://localhost:3000/api

# Production API URL (replace with your production domain)
PROD_API_URL=https://your-production-domain.com/api

# Android Emulator API URL
ANDROID_EMULATOR_API_URL=http://10.0.2.2:3000/api

# Local network API URL (replace with your computer's IP)
LOCAL_NETWORK_API_URL=http://192.168.1.100:3000/api

# App Configuration
DEV_APP_NAME=ERP System (Dev)
PROD_APP_NAME=ERP System

# Debug Configuration
DEV_DEBUG_MODE=true
PROD_DEBUG_MODE=false

# API Timeout (seconds)
API_TIMEOUT_SECONDS=30

# Pagination
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# Cache Configuration
CACHE_TIMEOUT_MINUTES=5

# Instructions for developers:
# 1. Copy this file to .env.local for local overrides
# 2. Update the IP addresses to match your network setup
# 3. For Android emulator, use 10.0.2.2 instead of localhost
# 4. For real devices, use your computer's IP address
